[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = {extras = ["all"], version = "*"}
pytest = "*"
pydantic = "*"

[dev-packages]
black = "*"

[requires]
python_version = "3.10"

[scripts]
server = "python run.py"
tests = "pipenv run pytest"
